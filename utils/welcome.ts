export const getWelcomeMessage = (() => {
  const messages = [
    'วันนี้ไม่สุดก็ซ่อมให้สุด 💪',
    'ล้มได้ แต่อย่าล้มเลิกนะเว้ย ✨',
    'เหนื่อยก็พัก แต่ห้ามพักความฝัน 😎',
    'ช้าแต่ชัวร์ ชัวร์กว่าไม่เริ่ม 🤏🚀',
    'ปัญหามีไว้แก้ แก้ไม่ได้ก็ยิ้มไว้ก่อน 😁',
    'วันนี้อาจบั๊ก พรุ่งนี้เดี๋ยวเทพ 🧩',
    'เก่งขึ้นทีละ 1% ก็ชนะเมื่อวานแล้ว 📈',
    'ใจเย็น ๆ น้ำเดือดก็ยังต้องใช้เวลา 🫧',
    'อย่ารออารมณ์ ทำก่อน เดี๋ยวอารมณ์ตาม 🔥',
    'ฝันใหญ่ แต่อัปเดตเล็ก ๆ ทุกวัน 🛠️',
    'แพ้ได้ แต่ห้ามแพ้ใจตัวเอง 💘',
    'ยากแค่ไหน ก็แค่ "ยัง" ทำได้ไม่พอ ⏳',
    'วันนี้ไม่เป๊ะ พรุ่งนี้ค่อยเป๊ะกว่าเดิม 🌤️',
    'ขอแค่เดินต่อ ก็ใกล้ขึ้นทุกก้าว 🥾',
    'ไม่ต้องดัง แค่ดังในใจตัวเองก็พอ 🤫✨',
    'เริ่มใหม่ได้เสมอ เวอร์ชันนี้ดีกว่าเดิม 🆙',
    'เหนื่อยได้ แต่อย่าดราม่ายาว นอนก่อนลุยต่อ 🛌⚡',
    'แผนใหญ่เริ่มจากงานเล็กที่ทำเดี๋ยวนี้ ⏱️',
    'วันนี้ทำให้เต็มที่ ที่เหลือปล่อยเวลาเดบัก 🕒🔧',
    'บอกตัวเองเบา ๆ ว่า "เราทำได้เว้ย" 🙌',
  ];

  let lastIndex = -1;
  return () => {
    if (messages.length === 1) return messages[0];
    
    // ป้องกัน infinite loop ด้วย fallback mechanism
    let idx = Math.floor(Math.random() * messages.length);
    let attempts = 0;
    const maxAttempts = 10; // จำกัดจำนวนครั้งที่พยายาม
    
    while (idx === lastIndex && attempts < maxAttempts) {
      idx = Math.floor(Math.random() * messages.length);
      attempts++;
    }
    
    // หากยังได้ค่าเดิม ให้ใช้ค่าถัดไป
    if (idx === lastIndex) {
      idx = (lastIndex + 1) % messages.length;
    }
    
    lastIndex = idx;
    return messages[idx];
  };
})();
